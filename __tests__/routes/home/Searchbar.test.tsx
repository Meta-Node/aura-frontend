describe('Search bar', () => {
  it('Should render the searchbar inside the homepage');

  it('Should filter the results based on the search');

  it('Should show the empty state while the search is not found');

  it('Should show all the connections when searchbar is empty');
});
