describe('Evaluation functionalities', () => {
  it('Should show the evaluation info if evaluated', async () => {});

  it('Should show the evaluation modal on click', async () => {});

  it('Should make an evaluation on click on modal', async () => {});

  it('Should show update evaluation if already evaluated', async () => {});
});
