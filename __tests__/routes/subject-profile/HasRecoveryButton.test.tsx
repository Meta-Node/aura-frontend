describe('Subject recovery functionality', () => {
  it('Should not have recovery badge', async () => {});

  it('Should have recovery badge', async () => {});

  it('on click recovery badge must navigate to the filtered view', async () => {});

  it('Should show the correct recovery connections', async () => {});
});
